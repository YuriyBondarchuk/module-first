import { Fragment } from "react";
import './Profile.scss';

function Profile(props: any) {
    return (
        <Fragment>
            work Profile
        </Fragment>
    );
}

export default Profile;